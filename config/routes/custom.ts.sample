import { TRoutes, createRoute } from './helper';

/**
 * Configures Routes to display in menu
 */
const CustomRoutes: TRoutes = [
  createRoute('menu:page.about', '/p/about', {
    fr: '/a-propos',
    en: '/about',
    es: '/a-proposito',
  }),
];

export default CustomRoutes;